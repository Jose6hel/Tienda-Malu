export async function renderProductDetail(container) {
    container.innerHTML = `
        <div style="padding: 24px 0;">
            <p style="color:var(--text-muted)">Detalle extendido del producto en desarrollo adaptativo...</p>
        </div>
    `;
}